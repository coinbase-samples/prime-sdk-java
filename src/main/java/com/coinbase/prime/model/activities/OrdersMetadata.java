package com.coinbase.prime.model.activities;

public class OrdersMetadata {
    public OrdersMetadata() {
    }

    public OrdersMetadata(Builder builder) {
    }

    public static class Builder {
        public OrdersMetadata build() {
            return new OrdersMetadata(this);
        }
    }
}
